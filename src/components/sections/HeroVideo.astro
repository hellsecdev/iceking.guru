---
import HeroVideoIntro from '../react/HeroVideoIntro';
import { asset, site } from '../../content/site';

const poster = asset('FROST-KING11.jpg');
const videoSrc = asset(site.heroIntroVideo);
---

<HeroVideoIntro client:only="react" videoSrc={videoSrc} posterSrc={poster} />
